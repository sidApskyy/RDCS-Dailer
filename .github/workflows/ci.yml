name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck

  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm test

  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run integration tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
          WEB_ORIGINS: http://localhost:3000
        run: pnpm --filter @rdcs/api exec jest test/integration --runInBand

  test-security:
    name: Security Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run security tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
          WEB_ORIGINS: http://localhost:3000
        run: pnpm --filter @rdcs/api exec jest test/security --runInBand

  test-csv-bullmq:
    name: CSV and BullMQ Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run CSV tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
        run: pnpm --filter @rdcs/api exec jest test/csv
      - name: Run BullMQ tests
        env:
          REDIS_URL: redis://localhost:6379/0
        run: pnpm --filter @rdcs/api exec jest test/bullmq

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

  test-phase4-integration:
    name: Phase 4 Integration Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run Phase 4 integration tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
          WEB_ORIGINS: http://localhost:3000
        run: pnpm --filter @rdcs/api exec jest test/integration/telephony.integration.spec.ts test/integration/telephony-concurrency.spec.ts test/integration/telephony-socket.integration.spec.ts test/integration/telephony-presence.spec.ts --runInBand

  test-phase4-compliance:
    name: Phase 4 Compliance Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run Phase 4 compliance tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
          WEB_ORIGINS: http://localhost:3000
        run: pnpm --filter @rdcs/api exec jest test/integration/telephony-compliance.spec.ts --runInBand

  test-phase4-security:
    name: Phase 4 Security Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run Phase 4 security tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
          WEB_ORIGINS: http://localhost:3000
        run: pnpm --filter @rdcs/api exec jest test/security/telephony-tenant-isolation.spec.ts test/security/telephony-rbac.spec.ts --runInBand

  test-phase4-e2e:
    name: Phase 4 Browser E2E Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rdcs_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium
      - name: Generate Prisma client
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:generate
      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:migrate:deploy
      - name: Seed test database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
        run: pnpm --filter @rdcs/database db:seed
      - name: Run Phase 4 E2E tests
        env:
          CI: 'true'
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/rdcs_test
          REDIS_URL: redis://localhost:6379/0
          JWT_SECRET: test-secret-key-with-sufficient-length-32+
          JWT_REFRESH_SECRET: test-refresh-secret-key-with-sufficient-length
          WEB_ORIGINS: http://localhost:3000
          NEXT_PUBLIC_API_URL: http://localhost:3001
          E2E_AUTH_DEBUG: '1'
        run: pnpm test:e2e
      - name: Upload E2E test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-results
          path: playwright-report/
          retention-days: 7
